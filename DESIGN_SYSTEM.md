# 张导插件商店设计系统

版本：2026-09-10（第二代视觉系统，配合完整界面重做）。实现文件：`src/styles/tokens.css`、`src/styles/base.css`、`src/styles/components.css`、`src/styles/markdown.css`；自动校验：`scripts/check-contrast.mjs`。

## 1. 项目与读法

Vue 3 + TypeScript 的 IPK 插件目录。主表面是面向访客的 Store（市场、详情、登录、投稿、个人中心），次表面是面向管理员的 Studio。读者需要快速判断「这个插件是什么、来自哪个仓库、有没有可用安装包、值不值得下载」。

设计目标：**像成熟厂商的产品页与后台，而不是脚手架页面**。具体表现为清晰的信息层级、克制的强调色、稳定的 8px 节奏、可预测的交互反馈、深浅双主题一致的质量，以及在 375–1440px 全区间都不塌陷的布局。

## 2. 品牌与概念

品牌标识是一枚「包裹」图形：圆角方形底 + 渐变 + 白色立体箱子轮廓，表达「封装好的安装包」。配色以**电光蓝 → 紫 → 青**的三段渐变作为唯一品牌渐变，只出现在标志、主标题强调文字、空状态与英雄区的环境光里，正文区域保持中性。

界面语言是简体中文，动词明确：`提交审核`、`收藏`、`下载 IPK`、`下载此文件`、`检查更新`。不做营销式假数据、不做「无病毒保证」、不做「手机号已认证」。

## 3. 色彩系统

单一强调色 + 中性灰阶 + 五个语义色。所有前景/背景组合均通过 `scripts/check-contrast.mjs` 的 4.5:1 校验（当前 34 组）。

| 角色 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| 页面底色 | `#f5f7fa` | `#080b12` | 页面背景 |
| 表面 | `#ffffff` | `#10151f` | 卡片、面板、表单 |
| 悬浮面 | `#f1f4f9` | `#1a2130` | 行悬浮、次级按钮 |
| 下沉面 | `#eaedf3` | `#0a0e16` | 代码块、内嵌证据 |
| 描边 | `#e3e8ef` | `#222b3b` | 卡片与输入边界 |
| 强调（填充） | `#2f5bff` | `#3d63ff` | 主按钮、焦点环 |
| 强调（文字） | `#2a4ee0` | `#7c9bff` | 链接、主色徽章 |
| 正文 | `#0f1729` | `#e8edf6` | 主要文本 |
| 辅助文本 | `#46536b` | `#9aa7bd` | 描述、次级文本 |
| 占位文本 | `#667085` | `#7c8aa3` | 占位、元信息 |
| 成功 | `#067647` / `#ecfdf3` | `#75e0a7` / `#06281c` | 已上架、校验通过 |
| 警告 | `#b54708` / `#fffaeb` | `#fec84b` / `#2a1d06` | 等待材料、待配置 |
| 危险 | `#b42318` / `#fef3f2` | `#fda29b` / `#2c1310` | 拒绝、失败、删除 |
| 信息 | `#175cd3` / `#eff8ff` | `#84caff` / `#101f35` | 进行中、说明 |

要点：

- **填充色与文字色分离**。`--primary` 用于填充按钮，`--primary-text` 用于白色或浅色底上的文字，因此深色主题下按钮可以提亮而链接仍然可读。`--primary-fill-hover/active` 与 `--primary-hover/active`（链接用）是两组不同令牌。
- 语义色一律「深文字 + 浅底 + 同色描边」三件套，避免只靠颜色区分状态：徽章里同时有文字与状态点。
- 品牌渐变 `--brand-gradient` 只用于文字与标志；页面环境光 `--hero-gradient` 是三层径向渐变的叠加，透明度低于 15%。
- 深色主题的描边与阴影独立定义，不是简单反色；深色不使用纯黑，浅色不使用纯白作为页面底色。

## 4. 排字

系统字体栈，不下载网络字体：`-apple-system, BlinkMacSystemFont, "SF Pro SC", "Segoe UI", "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", "Noto Sans SC", Roboto, sans-serif`；等宽用于版本号、commit、包名与哈希。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--text-display` | `clamp(2.125rem, 1.45rem + 2.6vw, 3.375rem)` | 市场主标题 |
| `--text-h1` | `clamp(1.625rem, 1.35rem + 1.1vw, 2.125rem)` | 页面标题 |
| `--text-h2` | `1.3125rem` | 区块标题 |
| `--text-h3` | `1.0625rem` | 卡片标题 |
| `--text-body` | `0.9375rem` | 界面正文 |
| `--text-read` | `1rem` | README 与长文阅读 |
| `--text-small` | `0.8125rem` | 辅助说明 |
| `--text-micro` | `0.75rem` | 标签、表格表头、元信息 |

标题字重 650–700，字距 `-0.02em` 至 `-0.038em`（越大越紧）；正文行高 1.6，README 行高 1.75 且限宽 78ch；数字统一使用 `tabular-nums`，避免统计数字跳动。

## 5. 空间、圆角与阴影

- 间距以 4px 为基准：4 / 8 / 12 / 14 / 16 / 18 / 20 / 22 / 26 / 32 / 40 / 72。
- 圆角：输入控件与小按钮 9px、卡片与面板 16px、重点容器（英雄区、详情头、抽屉）22px、徽章与开关 999px。
- 阴影五级 `--shadow-xs…xl`，全部为「大范围低透明度 + 小范围定位影」叠加；深色主题改用更高不透明度的黑色，避免灰雾。
- 焦点环统一 `--ring`（3px 强调色 22% 透明度）；危险操作用 `--ring-danger`。
- 玻璃效果只用于顶部导航与移动端下载条（`--glass-bg` + `backdrop-filter`），其余表面保持不透明以保证文字清晰。

## 6. 布局

- 内容宽度：主容器 `1200px`，Studio 与全局导航 `1360px`；桌面左右留白 48px，手机 32px。
- 顶栏高 68px，粘性 + 玻璃；`scroll-margin-top` 与粘性侧栏统一按 `--header-h` 计算。
- 市场：英雄区（渐变环境光 + 网格底纹，`mask-image` 淡出）→ 搜索 → 三条可信度说明 → 排序工具条 → 卡片网格（`minmax(330px, 1fr)`，自动 3/2/1 列）→ 分页。
- 详情：面包屑 → 信息头（身份、版本、收藏、下载、许可证四栏统计）→ 双栏（左：说明文档/安装包与校验/审核信息 三个标签页；右：粘性下载卡片 + 安全说明）。移动端在底部显示粘性操作条，不重复渲染下载按钮。
- 个人中心：档案头（字母标、掩码号码、三项统计）+ 两个标签页（我的提交 / 我的收藏）。
- Studio：左侧 252px 粘性导航（总览、插件管理、审核任务、操作日志、AI 设置、下载源、账号安全）+ 右侧内容区；≤1000px 收敛为可折叠的横向导航。
- 断点：1440 / 1200 / 1000 / 900 / 767 / 560。所有路由在 375/768/1280/1440 四种宽度下都通过无水平溢出断言。

## 7. 组件系统

自建轻量组件库，全部基于语义 HTML，不引入第二套 UI 框架。

| 组件 | 说明 |
| --- | --- |
| `AppIcon` | 24×24 线性图标，常量路径，`stroke=currentColor`，无第三方图标包 |
| `AppButton` | 6 种变体（primary / secondary / ghost / soft / danger / danger-solid）、3 种尺寸、加载态、可渲染为 link 或 router-link |
| `AppBadge` | 状态徽章：语义色 + 状态点 + 可选脉冲，用于所有状态展示 |
| `AppField` | 标签 + 控件 + 提示/错误；必填标记放在 `<label>` 之外，保证可访问名精确 |
| `AppModal` / `AppDrawer` | 危险操作确认与详情抽屉；Esc 关闭、滚动锁定、初始焦点 |
| `AppPagination` | 页码窗口 + 省略号 + 上下页，移动端自动收窄 |
| `AppSkeleton` / `AsyncState` | 加载骨架、错误重试、空状态统一出口 |
| `AppToaster` / `toast` | 右下角提示（成功/失败/警告/信息），自动消失且可手动关闭 |
| `CopyButton` | 复制 commit、SHA-256，带降级方案与提示 |
| `MonogramAvatar` | 由仓库名确定性推导的字母标与配色，不请求外部头像服务 |
| `PluginCard` | 市场卡片：整卡可点、按钮层级独立、悬停抬升 |
| `Studio*` | Studio 六个功能面板 |

交互状态：悬浮（背景/描边变化 + 卡片抬升 3px）、按下（下移 1px）、禁用（50% 透明度 + 禁止指针）、加载（内联转圈 + 文案切换）、成功/失败（Toast + 内联提示），全部不使用纯色闪烁或大面积动画。

## 8. 动效

时长 `120 / 180 / 260 / 420ms`，缓动统一 `cubic-bezier(0.16, 1, 0.3, 1)`（进场）与 `cubic-bezier(0.4, 0, 0.2, 1)`（往复）。动画仅用于：浮层进出、Toast、骨架微光、加载转圈、状态点脉冲、卡片悬浮。不使用滚动视差、入场逐项动画或轮播。`prefers-reduced-motion: reduce` 下全部时长归零。

## 9. 图标

31 个自绘线性图标（Lucide 风格几何），统一 24×24 视框、`stroke-width: 1.8`、圆角端点。图标永远与文字同色、永远 `aria-hidden`，因此可访问名只来自文字。状态图标与 `lib/status.ts` 的状态字典一一对应，避免同一个状态在不同页面使用不同符号。

## 10. 内容呈现

- **README 就是作者原文**。`lib/readme.ts` 用 marked 解析后经 DOMPurify 白名单净化，再用 URL 允许列表重写链接与图片（图片仅允许 `raw.githubusercontent.com`），并固定到读取时的 commit。排版层 `markdown.css` 提供 GitHub 风格的标题分隔线、代码块、表格与引用，同时保留品牌配色。
- 相对链接解析到 `github.com/{repo}/blob/{commit}/{path}`，外部链接新窗口打开并带 `rel="noopener noreferrer nofollow"`。
- 首页不展示 README；详情页才渲染，并标注快照路径、commit 与「作者原文，商店不改写」。
- 缺失内容如实提示（「作者尚未提供仓库描述」「仓库没有提供 README，商店不会代写说明内容」），不补写文案。

## 11. 可访问性

- 跳过导航链接、`main` 可聚焦、路由切换后焦点回到主区域。
- 每个页面单一 `h1`，标题层级不跳级；空状态标题使用真实标题元素。
- 所有可交互元素是 `a`、`button`、`input`、`select`、`summary` 或带 `role` 的控件；原生控件优先。
- 键盘：`/` 或 `⌘K` 聚焦搜索、`Esc` 关闭浮层、抽屉与模态有初始焦点、菜单按钮带 `aria-expanded`/aria-haspopup。
- 状态不只靠颜色：徽章始终带文字，进行中状态额外带脉冲点。
- 表单全部有可见标签，错误用 `role="alert"`，提示用 `aria-describedby` 关联。
- 34 组前景/背景对比度全部 ≥ 4.5:1，深浅主题各自独立校验。

## 12. 质量约束

不虚构统计、不承诺无病毒、不宣称手机号已验证、不使用「张导认证作者」。收录量、收藏数与下载数全部来自服务端真实聚合，且下载数口径在界面上明确标注为「下载尝试」。不引入评分、评论、购物车或安装功能。不复制上游站点的标志、字体或文案。

## 13. 与上游的关系

沿用 AstrBot 独立市场的浏览结构（搜索条、分页、卡片浏览、主题切换）与 GPL-3.0 授权，具体文件与修改说明见 `docs/UPSTREAM.md` 与 `THIRD_PARTY_NOTICES.md`。视觉层为本次完全重做，不再使用上游配色与样式。

## 14. 验证方式

- `node scripts/check-contrast.mjs`：解析 `src/styles/tokens.css`，校验 34 组颜色组合并写入 `docs/evidence/contrast.json`。
- `pnpm run test:e2e` 中的「设计系统、主题与键盘可用性」用例：断言令牌已生效（主色、圆角、正文字号、头部高度）、键盘焦点环可见、`/` 快捷键聚焦搜索、深色主题切换与持久化。
- 同一次运行的「手机、平板与桌面」用例：4 种宽度 × 6 条路由的无溢出断言，以及 Studio 六个面板与移动端面板的截图。
- 截图与检查记录：`docs/evidence/ui-*.png`、`docs/evidence/e2e-*.png`、`docs/evidence/frontend-qa.json`。

## 15. 更新策略

任何色彩、字阶、组件、布局或交互变更都必须同步更新本文件，并重新运行对比度校验与端到端用例。新增页面沿用现有令牌与组件，不得引入新的强调色或第二套设计语言。
