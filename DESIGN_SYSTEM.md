# 张导插件商店设计系统

更新：2026-09-11。本文件对应当前 Vue 实现；颜色与尺寸以 `src/styles/tokens.css` 为唯一来源。

## 1. 项目概况

张导插件商店是面向路由器用户的 IPK 插件目录，包含公开市场与管理员 Studio。平台为响应式 Web，使用 Vue 3、TypeScript、原生 CSS。主任务是浏览仓库、阅读说明、选择已审核安装包；管理任务是查看审核、处理插件、配置来源。此次范围为现有界面的整体打磨，保留业务与路由。

## 2. 品牌方向

概念：把「张导请回答」的三段信号标与深蓝、青色界面延伸为插件清单，让名称、来源、审核版本和下载操作形成稳定阅读顺序。保持紧凑、平静、直接的工具气质；不使用营销大标题和装饰渐变。

反向审查发现旧版的紫色 Studio 标识、市场卡片文档及缺失的浮层样式割裂了现有风格。已统一标识、列表层级和浮层组件。去掉品牌名后，信号标、深蓝底与青色状态仍与同品牌产品呼应；这是一致性设计，不声称其配色独占。

参考适配：

- [Primer Text input](https://primer.style/product/components/text-input/)：图标、输入区、清除动作各自占位，避免叠加定位挤压输入。
- [Carbon Empty states](https://carbondesignsystem.com/patterns/empty-states-pattern/)：区分加载、没有匹配项和读取失败；失败时不显示虚假零计数。
- [WAI-ARIA Tabs](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/)：标签与面板互相关联，方向键及 Home/End 切换。

文案使用简体中文、明确动作，如「提交审核」「下载 IPK」「重新加载」。README 保留作者原文。登录页按用户要求删除自填手机号提示框。空状态解释当前情况并提供下一步，错误展示原因与重试，加载状态不伪造数据。

## 3. 色彩系统

大面积深蓝/浅灰为主，中性色表面承载内容，青色仅强调交互。

| 角色 | 深色 | 浅色 |
| --- | --- | --- |
| 页面 / 下沉面 | `#07101b` / `#030812` | `#f4f7fa` / `#ffffff` |
| 表面 / 抬升 / 悬浮 | `#0e1a29` / `#152c41` / `#1d384f` | `#ffffff` / `#f1f6fa` / `#e6eef5` |
| 正文 / 次文字 / 辅助文字 | `#f3f8fd` / `#b7c6d5` / `#9cadbf` | `#08131f` / `#44576a` / `#596b7c` |
| 强调 / 悬浮 / 选中底 | `#5cdeff` / `#84e8ff` / `#173243` | `#0e7490` / `#155e75` / `#e0f1f7` |
| 强调及危险按钮文字 | `#050e17` | `#ffffff` |
| 成功 / 底 | `#6bd3a3` / `#073120` | `#0b6b45` / `#e6f4ee` |
| 警告 / 底 | `#f7c662` / `#2b2210` | `#8a5a00` / `#fdf3e0` |
| 危险 / 底 | `#ff9689` / `#451914` | `#b3261e` / `#fdeceb` |
| 中性状态 | `#8d9aa7` | `#556576` |

描边为深色 `rgba(85,122,145,.34)`、浅色 `rgba(14,60,80,.18)`；强调描边分别为 `rgba(111,167,193,.52)`、`rgba(14,60,80,.34)`。大面板通过背景区分；输入、表格行、代码与内容分隔仍使用细线。主题通过语义令牌切换，禁止逐组件反色。34 组文字组合经脚本校验，最低 4.62:1，见 `docs/evidence/contrast.json`。

## 4. 字体系统

正文及标题使用 `HarmonyOS Sans SC, MiSans, PingFang SC, Noto Sans SC, Microsoft YaHei, system-ui, sans-serif`；本机可用字体优先，不加载网络字体。等宽栈为 `SFMono-Regular, Cascadia Code, Roboto Mono, ui-monospace, Menlo, Consolas, monospace`，用于版本、哈希和统计。

字阶：h1 为 `clamp(1.5rem, 1.2rem + 1vw, 1.875rem)`；h2 18px；h3/正文 16px；辅助文字 14px；标签 13px。正文行高 1.6，标题 1.3，README 1.75。标题自然折行，长仓库名与哈希允许断行；表格的长内容允许在独立滚动区内查看。数字使用等宽数字。

## 5. 布局系统

主容器 74rem，宽容器 84rem，表单 42.5rem；左右安全距离至少 16px。间距使用 4/8/12/16/20/24/32/40/48/64px。桌面顶栏 60px，≤767px 拆成品牌操作行与导航行，总高 112px；页脚由全高 flex 布局自然推到底部。

市场顺序为标题和计数、搜索与排序、插件列表、分页。搜索最大宽度 38rem；手机独占一行，隐藏快捷键提示但保留键盘快捷操作。清单每行呈现名称、描述、来源、版本和统计，下载为次级操作；主要行动是打开详情后选择安装包。

详情由面包屑、身份信息、四项统计、内容标签与下载侧栏组成。≤1000px 合为单列，≤767px 固定底部下载操作并预留底部安全空间。个人中心使用提交与收藏两个标签，收藏延续清单样式。投稿和登录表单在手机优先显示输入，压缩辅助说明。

Studio 桌面为侧栏加工作区，≤1000px 使用折叠导航；总览统计桌面三列、≤560px 两列。表格在自己的容器滚动，菜单使用浏览器顶层避免裁切。无轮播、横向卡片轨道、全屏营销分区或结算流程。

## 6. 组件系统

统一使用自建 Vue 组件与原生 HTML，不引入额外 UI/图表库。

| 组件 | 约定 |
| --- | --- |
| AppButton | primary/secondary/ghost/soft/danger/danger-solid；加载时转圈并禁用；链接禁用时也阻止导航 |
| SearchToolbar | flex 独立图标、输入、清除/快捷键区；12px 间距；常规高48px，大号56px |
| PluginRow | 市场和收藏共用清单行；收藏请求中转圈，次级下载按钮 |
| AppField | 可见标签、必填标记、提示与错误；绑定 aria-describedby/aria-invalid |
| RepositoryForm | 仓库解析结果放在输入下方，避免覆盖长 URL |
| AppMenu | 原生 popover 顶层；边缘自动偏移；方向键、Home/End、Esc；最小12rem |
| AppDialog | 原生 dialog 模态；由 AppModal/AppDrawer 复用；限制视口高度，内容独立滚动，关闭后焦点恢复 |
| AppBadge | 状态文案与色彩共同表达，不只依赖色点 |
| AsyncState | 清单骨架、读取失败重试、空结果分别处理 |
| AppToaster | 固定提示，图标和文字、可关闭按钮，失败不静默 |
| AppHeader/AppFooter | 两种主题一致的信号标导航和来源说明；公开导航不新增 Studio 入口 |

触屏主要按钮、菜单项、清除按钮与折叠控件至少44px；输入通常48px。侧栏、标签、表单、浮层均复用现有语义和令牌。

## 7. 卡片与区块

清单优先，面板使用无外边框的同色面；控件圆角12px，按钮14px，分组16px，重点面板24px。普通卡片不投影；浮层用 `--shadow-dialog`，抬升表面用 `--shadow-shell`。不使用装饰玻璃或渐变文字。保留表单边界和表格分隔以帮助操作。

## 8. 图标系统

AppIcon 使用项目已有的24×24线性 SVG 路径，默认19px，辅助图标15–18px。颜色跟随 currentColor；装饰图标 aria-hidden；仅图标按钮必须有可访问名称。避免为普通状态额外增加插画。

## 9. 图片与资产

品牌使用 CSS 三段信号条加文字商标，公共导航与 Studio 一致。仓库使用确定性的字母头像。没有新增 AI 生成、图库或用户照片。README 图片遵循现有来源白名单和安全渲染规则；不可用资产不替换为虚构说明。

## 10. 动效与交互

仅 CSS 动效，时长160/220ms，缓动 `cubic-bezier(.2,.7,.2,1)`。悬浮用背景/文字变化，按下轻微位移，键盘用2px青色轮廓，禁用降透明度并阻止动作。进行中使用骨架或按钮转圈；成功与失败给出提示。无滚动入场、轮播和视差。

搜索同步 URL 时保持输入焦点；只有切换路径才移动主区域焦点。`/`、Cmd+K、Ctrl+K 聚焦搜索。标签页支持方向键及 Home/End；菜单支持方向键和 Esc。模态层使用原生焦点约束，点击遮罩或 Esc 可关闭。

`prefers-reduced-motion` 关闭动画并将程序滚动改为即时。路由按需加载，无新增字体/图形框架。LCP<2.5s、CLS<0.1、INP<200ms 为目标，未测量线上真实指标，不将构建或截图成功作为这些指标的证明。

## 11. 可访问性

使用语义链接、按钮、表单及 dialog/popover。跳过导航链接通往可聚焦 main；同路径筛选不抢焦点。标签和面板互相关联；提示、错误绑定控件；状态同时带文字。深浅主题分别校验正文≥4.5:1。

重点验证375/768/1280/1440px布局、弹窗焦点循环与恢复、菜单边缘定位、搜索连续输入。长内容允许换行；移动端安全区域预留。系统高对比度主要依赖原生控件及轮廓，未声称完成所有屏幕阅读器或浏览器兼容测试。

## 12. 项目质量约束

禁止虚假统计、评分和营销占位文案。禁止新增装饰渐变、卡片套卡片、无意义大图、第二种主按钮样式。浅色白色表面与白色按钮文字沿用已批准品牌；控件和表格细线是工具界面的明确例外。公共页、表单页、Studio 按任务组织，不套用营销页面强制分区数量。

验收入口：`pnpm run check` 校验 Vue/Worker 类型、lint、单元集成、热更新、导入工具、对比度和生产构建；`pnpm run test:e2e` 校验本地隔离的完整业务闭环与 UI 回归。测试凭据、GitHub 与 AI 数据仅为隔离夹具，截图不代表线上市场内容。

## 13. 后续页面规则

后续页面复用本文件的色彩、字阶、间距、组件和交互。所有新浮层复用 AppMenu/AppDialog；所有列表提供加载、空白、失败反馈。不要重新加入已删除的登录提示或恢复旧版紫色卡片设计。上游授权与来源继续维护在 `docs/UPSTREAM.md` 和 `THIRD_PARTY_NOTICES.md`。

## 14. 更新与证据

视觉规则改变时同步本文件与令牌；增加或改变交互时更新对应回归。`docs/evidence/contrast.json` 保存计算结果，`docs/evidence/frontend-qa.json` 保存浏览器检查记录，`docs/evidence/e2e-*.png` 和 `ui-*.png` 保存代表性截图。局部几何与遮挡问题必须看实际渲染；没有测量的性能或外部集成不写为通过。
